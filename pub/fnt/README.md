# Fonts

Faces added for jeddy, with where each one came from.

## SIL Open Font License 1.1

The licence body is in `OFL.txt`. Copyright notices:


## Notes

- `WorkSans.woff2` and `EBGaramond.woff2` are variable fonts (`wght` axis); the
  editor uses the default 400 instance.
- Merriweather is split into `latin`, `latin-ext`, `cyrillic` and `cyrillic-ext`
  subsets with `unicode-range`, because the full variable file is 4.6MB.
  The browser only fetches the subsets a document actually needs.
- Cyrillic coverage: iA Writer Quattro, EB Garamond, Merriweather and Mono F55
  cover Ukrainian. Atkinson Hyperlegible and Work Sans are Latin only.

The fonts that predate this list (Pixel Operator, iA Writer Duospace, Libre
Baskerville, Mono F55, Pinscher, Underwood Champion, Lekton) have no licence
recorded here.
